services:

  springboot:
    build:
      context: app
      dockerfile: $DOCKERFILE
    depends_on:
      - postgres
    ports:
      - $SPRING_LOCAL_PORT:$SPRING_DOCKER_PORT
    environment:
      POSTGRES_DOCKER_USER: $POSTGRES_USER
      POSTGRES_DOCKER_PASSWORD: $POSTGRES_PASSWORD
      POSTGRES_DOCKER_URL: jdbc:postgresql://database_cv24:5432/$POSTGRES_DB
    volumes:
      - .m2:/root/.m2
      - ./app:/app
    stdin_open: true
    tty: true


  postgres:
    image: 'postgres:latest'
    container_name: database_cv24
    environment:
      POSTGRES_DB: $POSTGRES_DB
      POSTGRES_PASSWORD: $POSTGRES_PASSWORD
      POSTGRES_USER: $POSTGRES_USER
    ports:
      - '5432:5432'
